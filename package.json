{
  "name": "rc2env",
  "description": "Convert rc's config object to env vars",
  "main": "index.js",
  "scripts": {
    "lint": "standard",
    "test": "tap test.js",
    "test:coverage": "npm run -s test -- --cov --coverage-report=lcov",
    "test:watch": "chokidar '**/*.js' -c 'npm run -s test' --initial -i node_modules -i coverage",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tlvince/rc2env.git"
  },
  "keywords": [
    "rc",
    "env",
    "dotenv"
  ],
  "author": "Tom Vincent <git@tlvince.com> (https://tlvince.com)",
  "engines": {
    "node": ">=4"
  },
  "bugs": {
    "url": "https://github.com/tlvince/rc2env/issues"
  },
  "homepage": "https://github.com/tlvince/rc2env#readme",
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "standard": "^5.4.1",
    "tap": "^5.7.1",
    "semantic-release": "^4.3.5"
  },
  "files": [
    "index.js"
  ],
  "license": "MIT"
}
